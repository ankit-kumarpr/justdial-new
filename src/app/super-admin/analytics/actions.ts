
'use server';

// This file is no longer needed as the data fetching has been moved to the client-side component.
// Keeping it empty to avoid breaking imports if it's referenced elsewhere, 
// but it can be safely deleted if no other files import from it.

    